horas = int(input("introduce horas trabajadas: "))
paga = int(input("introduce paga por hora: "))
paga_total = horas * paga
if horas > 40:
    paga_total *= 1.5
print("salario neto:", paga_total)
if paga_total == 1500 
    paga_total = paga_total * 25 / 100
    if paga_total >= 1100:
        print
